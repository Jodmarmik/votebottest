{
  "name": "vote-bot",
  "description": "Telegram bot for inline voting in groups and channels.",
  "repository": "https://github.com/yourusername/vote-bot",
  "keywords": ["telegram", "bot", "vote", "pyrogram", "mongo"],
  "env": {
    "BOT_TOKEN": {"description": "Your Telegram bot token", "required": true},
    "API_ID": {"description": "Telegram API ID", "required": true},
    "API_HASH": {"description": "Telegram API Hash", "required": true},
    "MONGO_URL": {"description": "MongoDB connection string", "required": true}
  },
  "buildpacks": [
    {"url": "heroku/python"}
  ]
}
